diff --git a/sdk-demo/src/main/java/co/imprint/sdkdemo/MainScreen.kt b/sdk-demo/src/main/java/co/imprint/sdkdemo/MainScreen.kt
index 98ae036..96e3c83 100644
--- a/sdk-demo/src/main/java/co/imprint/sdkdemo/MainScreen.kt
+++ b/sdk-demo/src/main/java/co/imprint/sdkdemo/MainScreen.kt
@@ -8,13 +8,18 @@ import androidx.compose.foundation.verticalScroll
 import androidx.compose.material3.*
 import androidx.compose.runtime.*
 import androidx.lifecycle.viewmodel.compose.viewModel
+import androidx.compose.ui.ExperimentalComposeUiApi
 import androidx.compose.ui.Modifier
 import androidx.compose.ui.graphics.Color
 import androidx.compose.ui.platform.LocalContext
+import androidx.compose.ui.platform.testTag
+import androidx.compose.ui.semantics.semantics
+import androidx.compose.ui.semantics.testTagsAsResourceId
 import androidx.compose.ui.text.input.KeyboardType
 import androidx.compose.ui.tooling.preview.Preview
 import androidx.compose.ui.unit.dp
 
+@OptIn(ExperimentalComposeUiApi::class)
 @Composable
 fun MainScreen(viewModel: MainViewModel = viewModel()) {
   val clientSecret by viewModel.clientSecret.collectAsState()
@@ -27,6 +32,7 @@ fun MainScreen(viewModel: MainViewModel = viewModel()) {
   ) { innerPadding ->
     Column(
       modifier = Modifier
+        .semantics { testTagsAsResourceId = true }
         .fillMaxSize()
         .padding(innerPadding)
         .padding(16.dp)
@@ -66,7 +72,8 @@ fun InputField(label: String, value: String, onValueChange: (String) -> Unit, ma
       onValueChange = onValueChange,
       modifier = Modifier
         .fillMaxWidth()
-        .height((maxLines * 24).dp),
+        .height((maxLines * 24).dp)
+        .testTag("client_secret_input"),
       maxLines = maxLines,
       keyboardOptions = KeyboardOptions.Default.copy(keyboardType = KeyboardType.Text)
     )
@@ -84,6 +91,7 @@ fun EnvironmentTabs(selectedEnv: Environment, onEnvSelected: (Environment) -> Un
       Tab(
         selected = env == selectedEnv,
         onClick = { onEnvSelected(env) },
+        modifier = Modifier.testTag("tab_${env.name.lowercase()}"),
         text = { Text(env.displayName) }
       )
     }
@@ -94,7 +102,9 @@ fun EnvironmentTabs(selectedEnv: Environment, onEnvSelected: (Environment) -> Un
 fun StartButton(onClick: () -> Unit) {
   Button(
     onClick = onClick,
-    modifier = Modifier.fillMaxWidth()
+    modifier = Modifier
+      .fillMaxWidth()
+      .testTag("start_application_button")
   ) {
     Text("Start Application")
   }
@@ -119,4 +129,4 @@ fun MainScreenPreview() {
   MaterialTheme {
     MainScreen(viewModel = MainViewModel())
   }
-}
\ No newline at end of file
+}
